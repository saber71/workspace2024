{
  "name": "server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "test": "vitest"
  },
  "peerDependencies": {
    "class-validator": "^0.14.1",
    "dependency-injection": "workspace:^"
  },
  "devDependencies": {
    "http-test": "workspace:*",
    "server-platform-express": "workspace:*",
    "server-platform-koa": "workspace:*"
  }
}

{
  "name": "server-user",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "test": "vitest",
    "serve:prod": "node dist/index.js",
    "serve:dev": "node --loader ts-node/esm --experimental-specifier-resolution=node src/index.ts --tsconfig tsconfig.json"
  },
  "devDependencies": {
    "server": "workspace:*",
    "server-platform-koa": "workspace:*"
  },
  "dependencies": {
    "mongoose": "8.2.0"
  }
}
